({
	/*onFacilitySelected : function(component, event, helper) {
		alert('IN onFacilitySelected');
        var facility = event.getParam('facility');
        alert('onFacilitySelected: facility = '+facility.name);
        if (facility) {
            component.set("v.facility", facility);
        }
	},*/
})